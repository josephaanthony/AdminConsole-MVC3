@model IEnumerable<AdminConsole.Models.TargetType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            Description
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TargetTypeCode }) |
            @Html.ActionLink("Details", "Details", new { id=item.TargetTypeCode }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TargetTypeCode })
        </td>
    </tr>
}

</table>
